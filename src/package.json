{
  "scripts": {
    "build": "npm run format && tsc && cp -f manifest.json styles.css threadloaf-128.jpg ../dist/",
    "format": "prettier --write \"**/*.ts\""
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "prettier": "^3.4.2",
    "typescript": "^5.3.3"
  }
}
